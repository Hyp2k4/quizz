// -------------------------------------------------------------
// TEMPORARY: TEST MODE RULES (ALLOW ALL)
// Use this to confirm the app works. Then switch to secure rules.
// -------------------------------------------------------------

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
